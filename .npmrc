# The @green-tea scope resolves from Verdaccio while core 26.9.0-beta.2 exists only there. The token is
# not here: it comes from ~/.npmrc locally and from the NPM_AUTH secret in CI. Plan B3 removes this line
# when the packages move to a core published on npm.
@green-tea:registry=https://npm.svc.zoit.services/
